const routes = {
    HOME_ROUTE: "/",
    WORK_ROUTE: "/work",
    SKILLS_ROUTE: "/skills",
    RESUME_ROUTE: "/resume",
    CONTACT_ROUTE: "/contact"
};

export const {
    HOME_ROUTE,
    WORK_ROUTE,
    SKILLS_ROUTE,
    RESUME_ROUTE,
    CONTACT_ROUTE
} = routes;

export default routes;