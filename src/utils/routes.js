const routes = {
    WORK_ROUTE: "/work",
    SKILLS_ROUTE: "/skills",
    CONTACT_ROUTE: "/contact"
};

export const {
    WORK_ROUTE,
    SKILLS_ROUTE,
    CONTACT_ROUTE
} = routes;

export default routes;